version: '3.8'

services:
  web:
    build: .
    container_name: php_apache_server
    ports:
      - "${PORT:-8040}:80"
    volumes:
      - ./src:/var/www/html
    environment:
      APACHE_DOCUMENT_ROOT: /var/www/html/public
    networks:
      - app-network
    user: "root" 

networks:
  app-network:
    driver: bridge
